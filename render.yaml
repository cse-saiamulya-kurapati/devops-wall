services:
  - type: web
    name: devops-wall
    runtime: static
    buildCommand: npm ci && node scripts/validate-contributors.mjs && npm run build
    staticPublishPath: dist
    pullRequestPreviewsEnabled: true
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
